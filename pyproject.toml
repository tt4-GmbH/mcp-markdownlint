[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mcp-markdownlint"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.1.2",
    "pymarkdownlnt>=0.9.24",
]

[tool.hatch.build.targets.wheel]
packages = ["src/Python"]